// Optional: Use injectEndpoints style for shared endpoints
// import { baseApi } from "./baseApi";

// Example:
// export const extendedApi = baseApi.injectEndpoints({
//   endpoints: (builder) => ({
//     // Shared endpoints here
//   }),
// });

// For now, endpoints will be defined in feature files using injectEndpoints
