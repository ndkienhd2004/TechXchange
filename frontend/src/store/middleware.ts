import type { Middleware } from "@reduxjs/toolkit";

// Optional: Add custom middleware here
// Example: logger, error handling, etc.

export const customMiddleware: Middleware[] = [
  // Add your custom middleware here
];
